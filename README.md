# LLMTgBot
# Telegram-бот с поддержкой контекста на основе локальной LLM

Telegram-бот, который использует локальную языковую модель через **LM Studio** для генерации ответов с учётом истории диалога.  
Бот запоминает контекст разговора **для каждого пользователя отдельно**, поддерживает управление сообщениями, очистку контекста и интеграцию с локальной языковой моделью.

---

## 📌 Возможности бота

- 🧠 **Сохраняет контекст диалога** для каждого пользователя  
- 🔄 Передаёт **полный контекст** в локальную LLM при каждом запросе  
- 🗑 Команда `/clear` — очищает историю диалога  
- 🧼 Команда `/del_message` — удаляет сообщения чата (контекст сохраняется)  
- 🚀 Команда `/start` — показывает описание бота и список доступных команд  
- 💬 Поддерживает независимые диалоги для разных пользователей  
- 🔐 Токен Telegram-бота вынесен в отдельный файл `config.py`  
- 📝 Логирование всех действий и ошибок в консоль  

---

## 📦 Требования

- **Python 3.9+**  
- **LM Studio** (локальный сервер LLM)  
- Языковая модель в формате **GGUF**  
  - Рекомендуемые модели:
    - `Qwen2.5-1.5B-Instruct-GGUF`  
    - `Qwen2.5-0.5B-Instruct-GGUF`  
    - `Phi-3-mini-4k-instruct-GGUF`  

---

## 🔧 Установка

### 1. Клонировать репозиторий

### 2. Установить зависимости

```bash
pip install -r lib.txt
```

### 3. Добавить токен бота
Открыть файл config.py и вставить токен:
```bash
TELEGRAM_BOT_TOKEN = "YOUR_BOT_TOKEN_HERE"
```

---

## 🚀 Запуск

### 1. Запустить LM Studio

- Открыть LM Studio

- Загрузить любую поддерживаемую GGUF модель

- Включить API-сервер

### 2. Запустить бота

```bash
python bot.py
```

### 3. Начать работу в Telegram

Открыть Telegram-бота по его имени: https://t.me/NLP_LLM_bot
